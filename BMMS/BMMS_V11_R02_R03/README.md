# R01反馈后：先R02，再单独提交R03

两份是独立单文件版本，不要拼接。每次只用所选文件替换官方 `code/kernel.asc`，
main/CMake保持比赛模板。ZIP是源码交付包，不是可直接上传的完整官方工程。

| 顺序 | 文件 | 验证的机制 |
|---|---|---|
| 1 | R02_MACRO_RING.asc | R01四个C块合为一次通知，减少宏块内部等待 |
| 2 | R03_RESIDUAL_DENSE.asc | 保持原R01，对其未覆盖的对齐密集shape补D01路径 |

**R03不包含R02。** 两者都从已确认15/15 Pass的R01独立派生。
R01同T31.8536分，P01历史32.0956；点9–11改善被点12回退基本抵消。
现阶段不能宣称新路线全面胜出。

R02不改变shape guard、调度或算术；每核组GM ring从64KiB增至256KiB，
片上buffer不增加。一个完整128×256宏块仍写4次Fixpipe，但READY从4次减为1次，
两个AIV各等1次，读完各自完整数据才释放。实际速度和更大工作集的影响待平台验证。

R03入口按R01→残余D01→P01执行。新增分支严格限定`D01Eligible && !R01Eligible`，
包括一部分M<128/N<256或宏块并行度不足的对齐shape。补充小输出Mmad的PIPE_M
依赖。它只依赖公开形状，不能保证点15一定命中，也不根据隐藏点号选实现。

离线检查：每版169次普通源码运行、85次重复位比较、4494个计划检查、7560个
分支边界检查；三版所测输出逐位一致。R03模型96次调用原R01、76次调用新增分支
（含3个已知数值限制调用）。新增环槽读写所有权检查，并成功拒绝提前FREE的错误版本。
这些是CPU源码模型，不认证CANN编译、硬件缓存可见性或性能。3个极端数值限制仍保留。

用户当前无测试环境，所以本轮只准备提交、未占用NPU。收到结果时请注明R02/R03，
保留15点完整耗时；若编译/精度失败则保留原始报错。按同T计算总分，不只挑加速点。

R02 SHA256：`7e0c3e51e763170973c97d85836b1d217c0746da063352c5e392459a669220e5`

R03 SHA256：`233cb1d95ab788338958f6747a662578c5b1ee0e7e05ba8bef9d26f832c04fac`

完整设计和源码差分见仓库 `V11_followup/`，R01截图及逐点分数在 `V11_results/`。
