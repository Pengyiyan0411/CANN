# V11：本轮提交 R01_REUSE_2X2

只将 **R01_REUSE_2X2.asc** 的内容替换官方工程的 `code/kernel.asc`，按原方式提交。
不要同时拼接 P01，不要修改官方 main/CMake。ZIP 是源码交付包，不是官方完整工程。
`P01_CONTROL.asc` 是冻结对照，本轮默认先提交 R01。请保留本次提交编号和完整15点结果。

| 文件 | 用途 | 设备状态 |
|---|---|---|
| R01_REUSE_2X2.asc | 新的密集宏块输入复用，其他分支继承P01 | 待平台编译、精度与计时 |
| P01_CONTROL.asc | 原P01对照，原件SHA不变 | 有历史15点记录，非本轮重测 |

R01 SHA256：`dc372a48a8b38dcb3ee1c782f93842624945cfe0627b04b2112e70d0a949663e`。

本版改变：128×256宏块内，四个64×128输出块共享已搬入的输入；
L1的K块为256，L0的K块为64。只有对齐且宏块数量足够占满可用AIC时进入新分支。
完整K累加完才取max，不改变max/sum次序，不做split-K，也不转成FP16 C。
小规模、特殊shape和低并行度情况仍用P01原有代码，原函数体已作恢复比对。

离线证据：153次正常源码CPU运行、4494个计划检查通过，77次重复输出位比较一致。
两种dtype、四布局、全负/全零、K/M/N尾块、N split与环区credit有覆盖。
在命名的128×256×256单核组fixture中，D01→R01的逻辑输入字节393216→196608，
GM→L1次数16→2，L0输入字节同样减半；Mmad次数8→16，C输出字节不变。
这证明代码中的复用生效，**不等于硬件提速**。

本地没有CANN/NPU，CPU模型不能验证实际指令、缓存可见性、Cube舍入或性能。
3个已知FP32消去/BF16中间溢出反例仍能复现；15点Pass也不能证明全数值域精度。
该文件为用户授权的平台实验候选，尚非已获提速的最终版本。

F01已确认15/15 Pass，但同T复算21.1215，低于P01历史32.0956，停止作为主线。
F02未测；不再默认要求提交它。详细原因在仓库的V10_results与V11_impl/DESIGN.md。

用户当前没有测试环境，因此本轮先平台提交R01。另备有20分钟时限P01/R01
诊断包，后续需要时使用；本轮未占用算力。
